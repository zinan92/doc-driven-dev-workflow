# Example Task

This example shows what a completed document-driven task looks like.

Use it to understand:

- task folder shape
- handoff naming
- how PRD, user flow, planning, execution, and review connect
- what a finished `status.md` and `system/state.json` look like

This is a reference artifact, not a live task.
